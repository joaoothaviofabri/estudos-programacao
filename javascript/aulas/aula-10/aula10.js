let num = 10
let num2 = 5
res = (! (num % 2) ? "Par" : "Impar")
console.log(res)

let st = "A"
res = (st == "A" ? "Ativo" : "Inativo")
console.log(res)

//0 = False
//1 - True

// Teste lógico ? se verdadeiro : se falso

